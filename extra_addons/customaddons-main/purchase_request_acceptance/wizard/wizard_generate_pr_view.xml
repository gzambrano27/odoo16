<odoo>
  <record id="view_wizard_generate_pr_form" model="ir.ui.view">
    <field name="name">wizard.generate.pr.form</field>
    <field name="model">wizard.generate.pr</field>
    <field name="arch" type="xml">
      <form string="Seleccionar Productos a Aceptar">
        <group>
          <field name="request_id" readonly="1"/>
        </group>
        <field name="line_ids">
          <tree editable="bottom">
            <field name="product_id"/>
            <field name="product_uom_id"/>
            <field name="quantity_requested"/>
            <field name="quantity_to_accept"/>
            <field name="tipo_costo"/>
            <field name="rubro"/>
            <field name="costo_uni_apu"/>
          </tree>
        </field>
        <footer>
          <button string="Confirmar y Crear Recepción" type="object" name="action_generate_pr_acceptance" class="btn-primary"/>
          <button string="Cancelar" special="cancel" class="btn-secondary"/>
        </footer>
      </form>
    </field>
  </record>
</odoo>
