<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_wizard_cancel_meeting_form" model="ir.ui.view">
            <field name="name">wizard.cancel.meeting.form</field>
            <field name="model">wizard.cancel.meeting</field>
            <field name="arch" type="xml">
                <form string="Cancelar Reunión">
                    <field name="meeting_id" invisible="1"/>
                    <field name="reason" placeholder="Por favor, introduce el motivo de la cancelación..."/>
                    <footer>
                        <button name="action_confirm_cancel" string="Confirmar Cancelación" type="object" class="btn-primary" data-hotkey="q"/>
                        <button string="Descartar" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_wizard_cancel_meeting" model="ir.actions.act_window">
            <field name="name">Cancelar Reunión</field>
            <field name="res_model">wizard.cancel.meeting</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>