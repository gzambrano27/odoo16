<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista del wizard -->
    <record id="view_sale_order_import_wizard_form" model="ir.ui.view">
        <field name="name">sale.order.import.wizard.form</field>
        <field name="model">sale.order.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Importar Líneas de Pedido" enctype="multipart/form-data">
                <group>
                    <field name="sale_order_id" readonly="1"/>
                    <field name="filename" invisible="1" />
                    <field name="file" filename="filename"/>
                </group>
                <footer>
                    <button string="Importar" type="object" name="action_import_lines" class="btn-primary"/>
                    <button string="Cancelar" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Acción para abrir el wizard desde el pedido -->
    <record id="action_sale_order_import_wizard" model="ir.actions.act_window">
        <field name="name">Importar Líneas de Pedido</field>
        <field name="res_model">sale.order.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_sale_order_import_wizard_form"/>
        <field name="target">new</field>
        <field name="context">{'default_sale_order_id': active_id}</field>
    </record>

    <!-- Botón en la vista del pedido de venta -->
    <record id="sale_order_view_form_inherit_import_excel" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.import.excel</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_sale_order_import_wizard)d"
                        type="action"
                        string="Importar Excel"
                        class="btn-secondary"
                        attrs="{'invisible': [('state', 'not in', ('draft'))]}" />
            </xpath>
        </field>
    </record>
</odoo>
