<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<record id="action_product_pricelist_inherit_view" model="ir.actions.act_window">
		<field name="name">action.product.pricelist.inherit.view</field>
		<field name="res_model">product.pricelist</field>
		<field name="view_mode">tree,kanban,form</field>
		<field name="context">{}</field>
		<field name="domain">[('import_data', '=', True)]</field>
		<field name="view_id" ref="product.product_pricelist_view_tree"/>
		<field name="search_view_id" ref="product.product_pricelist_view_search"/>
		<field name="help" type="html">
			<p class="o_view_nocontent_smiling_face">
				Create a price list
			</p>
		</field>
	</record>
</odoo>
