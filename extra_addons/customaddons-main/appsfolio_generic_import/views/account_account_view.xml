<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<record id="action_account_account_inherit_view" model="ir.actions.act_window">
		<field name="name">action.account.account.inherit.view</field>
		<field name="res_model">account.account</field>
		<field name="view_mode">tree,kanban,form</field>
		<field name="context">{}</field>
		<field name="domain">[('import_data', '=', True)]</field>
		<field name="view_id" ref="account.view_account_list"/>
	</record>
</odoo>
