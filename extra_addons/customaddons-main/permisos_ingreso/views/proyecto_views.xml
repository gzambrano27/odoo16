<odoo>
    <!-- Vista de formulario para el modelo 'Proyecto' -->
    <record id="view_form_proyecto" model="ir.ui.view">
        <!-- Nombre de la vista de formulario -->
        <field name="name">proyecto.form</field>
        <!-- Modelo al que está asociada la vista -->
        <field name="model">permisos_ingreso.proyecto</field>
        <!-- Estructura XML de la vista de formulario -->
        <field name="arch" type="xml">
            <form string="Proyecto">
                <sheet>
                    <group col="12">
                        <group colspan="6">
                            <!-- Campo para ingresar el nombre del proyecto -->
                            <field name="nombre"/>
                        </group>
                        <group colspan="6">
                            <!-- Campo para ingresar la ubicación del proyecto -->
                            <field name="ubicacion"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user" options="{'post_refresh': 'recipients'}"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Vista de lista para el modelo 'Proyecto' -->
    <record id="view_tree_proyecto" model="ir.ui.view">
        <!-- Nombre de la vista de lista -->
        <field name="name">proyecto.tree</field>
        <!-- Modelo al que está asociada la vista -->
        <field name="model">permisos_ingreso.proyecto</field>
        <!-- Estructura XML de la vista de lista -->
        <field name="arch" type="xml">
            <tree string="Proyectos" default_order="create_date desc">
                <!-- Campos para mostrar el nombre y la ubicación del proyecto -->
                <field name="nombre"/>
                <field name="ubicacion"/>
                <field name="create_uid" widget="many2one_avatar_user" string="Creado por" optional="show"/>
                <field name="create_date" string="Fecha de creación" optional="show"/>
            </tree>
        </field>
    </record>

    <!-- Acción para abrir la vista de lista y formulario del modelo 'Proyecto' -->
    <record id="action_proyectos" model="ir.actions.act_window">
        <!-- Nombre de la acción que aparecerá en el menú -->
        <field name="name">Proyectos</field>
        <!-- Modelo que se abrirá con esta acción -->
        <field name="res_model">permisos_ingreso.proyecto</field>
        <!-- Modo de vista (lista y formulario) al abrir la acción -->
        <field name="view_mode">tree,form</field>
    </record>


</odoo>
