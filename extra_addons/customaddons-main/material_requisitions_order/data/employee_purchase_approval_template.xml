<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="custom_email_purchase_requisition_iruser_custom" model="mail.template">
            <field name="name">Department Approval Work Order - Send by Email</field>
            <!-- <field name="email_from">${object.approve_manager_id.work_email}</field>
            <field name="subject">Department Approval Work Order - ${object.name}</field>
            <field name="email_to">${object.employee_id.work_email}</field> -->
            <field name="email_from">{{object.approve_manager_id.work_email}}</field>
            <field name="subject">Department Approval Work Order - {{object.name}}</field>
            <field name="email_to">{{object.employee_id.work_email}}</field>
            <field name="model_id" ref="model_material_requisition_order"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html" type="html">
                <!-- <![CDATA[ -->
                <div style="font-family: 'Lucida Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF;">
                    <p>Dear <t t-esc="object.employee_id.name"/>,</p><br/>
                    <p>This mail is to inform you that material requisition request number <t t-esc="object.name"/> is approved by the department manager.</p> <br/>
                    <p>Do not hesitate to contact us if you have any questions.</p>
                    <br/>
                </div>
                <!-- ]]> -->
            </field>
        </record>
        
    </data>
</odoo>
